<!--<div class="photos-wide">-->
<div class="photos-wide">
    <div class="swiper-container c-arrows">
        <div class="swiper-wrapper">
            <?php foreach($photos as $index => $photo): ?>
            <div class="swiper-slide">
                <div class="slide-wrapper">
                    <a class="img zoomlink" rel="imagelightbox" href="<?php echo asset($photo->img_orig); ?>" style="background-image:url(<?php echo asset($photo->img_orig); ?>)" data-src="background-image:url(<?php echo asset($photo->img_orig); ?>)"></a>
                </div>
            </div>
            <?php endforeach; ?>
        </div>
        <figure class="ui-arrow-next ui-arrow"></figure>
        <figure class="ui-arrow-prev ui-arrow"></figure>
        <div class="swiper-pagination"></div>
    </div>
</div>
